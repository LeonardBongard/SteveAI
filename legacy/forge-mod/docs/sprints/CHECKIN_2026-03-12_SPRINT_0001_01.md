# Sprint Check-In

## Check-In
1. Date:
- 2026-03-12
2. Sprint:
- `SPRINT_0001_RUNTIME_REPLANNING_SPINE`
3. Current Sprint Goal:
- Steve can detect no-progress and perform structured runtime replanning without stalling.
4. Current Slice:
- `SPRINT_0001_SLICE_01_STUCK_SIGNALING_AND_LOCAL_REEVALUATION`

## What Changed
1. Increment or observation:
- Added reusable `StuckReason` taxonomy and `LocalReevaluationPolicy`.
- Wired mining to rescan while moving, detect stuck categories, and switch to better visible targets before continuing stale search paths.
- Wired pathfinding to emit structured stuck reasons and expose them in action descriptions.

## Review
1. What passed:
- Main sources compile with the new stuck/re-evaluation layer.
- Focused unit tests for retarget policy pass.
- Existing behavior scheduler regression tests still pass.
2. What failed:
- No manual in-game validation yet in this check-in.
3. Evidence:
- commands:
  - `./gradlew test --tests com.steve.ai.action.search.LocalReevaluationPolicyTest --tests com.steve.ai.execution.behavior.BehaviorSchedulerTest --tests com.steve.ai.execution.behavior.BehaviorRegistryTest`
- logs:
  - build/test completed successfully
- scenarios:
  - manual `iron_pickaxe` and visible-candidate search scenario still pending

## Retrospective
1. What improved globally:
- Stuck detection and local target refresh are now framed as reusable control-layer behavior instead of ore-only heuristics.
2. What drifted local:
- Current integration is strongest in mining/pathing first; full runtime replan insertion is still pending.
3. What should change next:
- Validate in-game and then move to structured stuck-context snapshot + replan request pipeline.
